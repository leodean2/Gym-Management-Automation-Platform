// Global test setup — currently minimal, extended if/when we need
// shared custom matchers or global hooks. Loading TEST_DATABASE_URL
// happens in helpers/testDb.js instead, not here, so any test file can
// import that helper directly without relying on setup-file ordering.
