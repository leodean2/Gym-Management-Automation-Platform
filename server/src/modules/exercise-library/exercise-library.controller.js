const exerciseLibraryService = require('./exercise-library.service');
const { ok, created } = require('../../lib/apiResponse');

// Feature 9 — Exercise Library Management
// Controllers stay thin: validate input, call the service, shape the
// response envelope. Business logic belongs in exercise-library.service.js.

// TODO: implement controller functions matching the routes in
// exercise-library.routes.js, following auth.controller.js as the reference pattern.

module.exports = {
  // e.g. list, getById, create, update, ...
};
